"""Cache-only scalar theory analysis. Import concrete modules explicitly.

The package initializer deliberately imports neither Torch nor model code so a
copied scalar bundle can be plotted in an environment without model packages.
"""
